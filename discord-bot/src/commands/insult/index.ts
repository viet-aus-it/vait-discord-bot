import { Message, TextChannel } from 'discord.js';

const FETCH_URL = 'http://www.pangloss.com/seidel/Shaker/';

const insult = async ({
    content,
    channel,
    id,
    reference,
    author,
}: Message) => {
    
}