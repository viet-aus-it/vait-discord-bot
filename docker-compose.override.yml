version: "3.8"

services:
  bot:
    build:
      target: development
    container_name: discord_bot_dev
    image: viet-aus-it/discord_bot:development
    restart: unless-stopped
    volumes:
      - ./discord-bot:/src
      - "src/node_modules"

  # dashboard:
  #   build:
  #     target: development
  #   container_name: dashboard_dev
  #   image: viet-aus-it/dashboard:development
  #   restart: unless-stopped
  #   volumes:
  #     - ./dashboard:/var/www/
  #     - "/var/www/html"

  # Database service should go here for local testing
