version: "3.8"

services:
  bot:
    build:
      context: "./discord-bot"
      dockerfile: ".docker/Dockerfile"

  # dashboard:
  #   build:
  #     context: "./dashboard"
  #     dockerfile: ".docker/Dockerfile"
  #   env_file: "dashboard/.env"
